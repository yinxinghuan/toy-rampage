import test from 'node:test';import assert from 'node:assert/strict';
import {BattlePresentation,flightTime,projectilePoint,PRESENTATION_LAG} from '../src/pixel/battle-presentation.js';
test('solid projectiles have flight time; rails stay instant at the presentation contact',()=>{for(const k of['spring','mortar','bubble']){const t=flightTime(k,90,400);assert(t>=.14&&t<=PRESENTATION_LAG);}assert.equal(flightTime('rail',200,400),0);assert.equal(flightTime('fusion',200,400),0);assert(flightTime('bubble',20,400)>flightTime('spring',20,400));});
test('projectile paths reach exact contact, mortar arcs without drawing a full trajectory',()=>{const a={x:10,y:100},b={x:100,y:10};assert.deepEqual(projectilePoint(a,b,0,'mortar',400),a);assert.deepEqual(projectilePoint(a,b,1,'mortar',400),b);assert(projectilePoint(a,b,.5,'mortar',400).y<55);assert.equal(projectilePoint(a,b,.5,'mortar',400,true).y,55);assert.equal(projectilePoint(a,b,.5,'spring',400).y,55);});
test('HP, death and result snapshots remain pre-hit until contact; paused capture cannot push contact away',()=>{const g={elapsed:0,hp:100,kills:0,stage:'wave',choices:[],coins:0,enemies:[{id:1,hp:10,d:4}]},p=new BattlePresentation(g);g.elapsed=1;g.kills=1;g.enemies=[];g.stage='win';p.capture(g,1);assert.equal(p.view(1.1).kills,0);assert.equal(p.view(1.1).enemies[0].hp,10);assert.equal(p.settled(1.1),false);p.capture(g,1.2);assert.equal(p.settled(1.22),true);assert.equal(p.view(1.22).kills,1);assert.equal(p.view(1.22).enemies.length,0);});
